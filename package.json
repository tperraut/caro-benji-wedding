{
    "name": "caro-benji",
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "preview": "vite preview",
        "clean": "rm -rf dist",
        "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
        "check": "tsc"
    },
    "dependencies": {
        "kaplay": "^3001.0.14"
    },
    "devDependencies": {
        "@types/node": "^22.14.1",
        "typescript": "^5.8.3",
        "vite": "^6.3.2"
    }
}